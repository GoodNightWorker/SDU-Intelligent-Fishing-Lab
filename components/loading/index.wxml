<view class="mask" catch:tap="tap" catch:touchmove="touchMove"></view>
<view class="container">
    <ul class="strip-loading">
        <li class="item" wx:for="{{6}}" wx:key="index" style="--line-index: {{index}};"></li>
    </ul>
    <text>{{title}}</text>
</view>